# הודעה ליואכים - עדכון על מערכת ה-DS ותיקוני קוד

שלום יואכים,

רציתי לעדכן אותך בקצרה לגבי האופן שבו ייצרתי את ההחלטות של מערכת ה-DS בקובצי ה-CSV, ולשאול אם לדעתך נכון להישאר עם ההגדרה הנוכחית או לעבור לקריטריון מותנה יותר.

## 1. שינוי כלל ההחלטה של מערכת ה-DS

### המודל המקורי (בייסיאני)
בהתחלה השתמשתי בכלל החלטה בייסיאני שמבוסס על ההסתברות האפריורית לאירוע `ps` ועל פרמטר הרגישות של המערכת `d′_s`. עבור כל גירוי של המערכת `s_t` חישבתי את:

```
logit P(Signal | s_t) = ln(ps / (1 − ps)) + d′_s · s_t
```

ואז את:

```
P(Signal | s_t) = 1 / (1 + exp(−logit))
```

במודל המקורי המערכת סימנה "signal" כאשר `P(Signal | s_t) > 0.5`, אחרת "noise".

### הבעיה שזיהינו
כשעברתי על הקבצים ראינו שיוצא שבשילובים מסוימים של `ps` נמוך (למשל 0.2) ו-`d′_s` נמוך (0.5), המערכת כמעט אף פעם לא אומרת "signal", ולכן בפועל אי אפשר לדבר על `d′` סביר כי אין כמעט TP.

### הפתרון הנוכחי (סף סימטרי)
אחרי שיחה עם יואב החלטנו לפשט את כלל ההחלטה, ולעבור לסף סימטרי על `s_t` עצמו, בלי תלות ב-`ps`. כרגע ההחלטה היא פשוט:

- `s_t > 0` ⇒ החלטת המערכת "signal"
- `s_t ≤ 0` ⇒ החלטת המערכת "noise"

מכיוון שבקוד אני מוסיף לכל הגירויים של האדם והמערכת סקלר של 6.5 (כדי לעבוד עם מספרים "נחמדים" יותר למשתתף), זה שקול לכלל:

- `s_t + 6.5 > 6.5` ⇒ "signal"

עדכנתי לפי זה את העמודות `ds_dec_t01`–`ds_dec_t120` בשני קובצי ה-CSV, ושמרתי גיבוי של הגרסה הישנה עם הכלל הבייסיאני.

### יתרונות וחסרונות
הפתרון הנוכחי פשוט, עקבי, ולא תלוי ב-`ps`, אבל מצד שני הוא לא לוקח בחשבון מטריצת עלויות או קריטריון אופטימלי. בשלב הזה זה נראה לנו מספיק לניסוי הפיילוט, אבל לפני שממשיכים הייתי שמח לשמוע את דעתך: האם עדיף להשאיר כרגע את הסף הסימטרי, או שכבר עכשיו היית מעדיף שאגדיר קריטריון אופטימלי שמבוסס גם על `ps` וגם על עלויות השגיאה?

---

## 2. תיקוני באגים חשובים בקוד הניסוי

במהלך הבדיקות זיהיתי ותיקנתי מספר בעיות קריטיות בקוד:

### א. תיקון חלוקת הבלוקים (Block 3)
**הבעיה:** Block 3 היה משתמש בטריאלים 1-100 מהשורה, מה שגרם לחפיפה עם Block 1 (טריאלים 1-10) ו-Block 2 (טריאלים 11-20).

**התיקון:** Block 3 עכשיו משתמש בטריאלים 21-120 מהשורה:
- Block 1: טריאלים 1-10 (CSV columns `event_t01` עד `event_t10`)
- Block 2: טריאלים 11-20 (CSV columns `event_t11` עד `event_t20`)
- Block 3: טריאלים 21-120 (CSV columns `event_t21` עד `event_t120`)

זה מבטיח שכל משתמש רואה 120 טריאלים ייחודיים ללא חפיפות.

### ב. תיקון שמירת משתמשים ו-CSV rows
**הבעיה:** 
- משתמשים יכלו לקבל שורות CSV שונות אם רעננו את הדף
- לא הייתה מעקב אחר איזו שורת CSV שויכה לכל משתמש
- שורות CSV לא סומנו כ-"used" כשמשתמש סיים

**התיקון:**
- כל משתמש מקבל `csv_row_id` שמזהה את השורה שלו ב-CSV
- השורה נשמרת ב-`ExperimentData` ומשויכת למשתמש כבר מההתחלה
- כשמשתמש מסיים את הניסוי, השורה ב-CSV מסומנת כ-`used=1`
- נוסף שדה `isDemo` שמבדיל בין משתמשי פיילוט (1) למשתמשי CloudResearch (0)

### ג. תיקון שמירת נתונים עקביים
**הבעיה:** אם משתמש רענן את הדף, הוא יכול היה לקבל שורה חדשה מה-CSV, מה שגרם לחוסר עקביות בין הנתונים ב-DB לבין מה שהמשתמש ראה.

**התיקון:**
- השורה נבחרת פעם אחת בלבד (בדף הנחיתה הראשוני)
- השורה נשמרת ב-session וב-DB
- אם המשתמש חוזר, הוא מקבל את אותה שורה (לפי `csv_row_id`)

### ד. תיקון שדות ps, dprime_h, dprime_s
**הבעיה:** השדות ב-DB לא תמיד התאימו לשורה ב-CSV.

**התיקון:** השדות `ps`, `human_sensitivity` (dprime_h), ו-`ds_sensitivity` (dprime_s) נשמרים ישירות מהשורה ב-CSV ומתעדכנים אוטומטית.

---

## סיכום השינויים

✅ **CSV files:**
- עדכון כל עמודות `ds_dec_t01`–`ds_dec_t120` לסף סימטרי (`s_t > 0`)
- גיבוי של הגרסה הישנה: `*-old_baysian_ds.csv`

✅ **Code fixes:**
- Block 3 משתמש בטריאלים 21-120 (ללא חפיפה)
- כל משתמש מקבל `csv_row_id` קבוע
- שורות CSV מסומנות כ-`used=1` כשמשתמש מסיים
- שדות `ps`, `dprime_h`, `dprime_s` נשמרים נכון מהשורה ב-CSV

✅ **Data consistency:**
- כל משתמש רואה 120 טריאלים ייחודיים
- הנתונים ב-DB תואמים למה שהמשתמש ראה
- מעקב מלא אחר איזו שורה שויכה לכל משתמש

---

תודה רבה,

עמרי

