# Experiment Project Guide: Structure & Adaptation

## 📁 Project Structure

```
purchase-of-info-omri/
│
├── 📂 PurchaseodInfo/          # Django project configuration
│   ├── settings.py             # Main settings (database, apps, etc.)
│   ├── urls.py                 # Root URL routing
│   └── wsgi.py                 # Web server interface
│
├── 📂 experiment/              # Main Django app (your experiment logic)
│   ├── models.py               # Database models (ExperimentData, ExperimentAction, TOASTResponse)
│   ├── views.py                # Backend logic (page handlers, data loading)
│   ├── urls.py                 # URL routing for experiment pages
│   └── migrations/             # Database schema changes
│
├── 📂 templates/               # HTML pages (what users see)
│   ├── landing_page.html       # Welcome page
│   ├── consent_form.html       # Consent form
│   ├── instructions.html       # Experiment instructions
│   ├── game.html               # Main game/trial page
│   ├── toast_1.html            # Post-experiment questionnaire (part 1)
│   ├── toast_2.html            # Post-experiment questionnaire (part 2)
│   └── end.html                # Thank you page
│
├── 📂 static/                  # Static files (CSS, images, fonts)
│   ├── images/                 # Images (glucometer, alarms, etc.)
│   └── fonts/                  # Custom fonts
│
├── 📂 data/                    # Data files
│   ├── conditions_experiment_3ps_11x11_120_A.csv  # Main experiment data
│   ├── experiment_data.csv     # Output: participant data
│   ├── experiment_actions.csv  # Output: all trial responses
│   └── TOAST.csv               # Output: questionnaire responses
│
├── manage.py                   # Django management script
├── db.sqlite3                  # SQLite database (stores all data)
└── requirements.txt            # Python dependencies
```

---

## 🔄 How It Works: Experiment Flow

### **1. Landing Page** (`landing_page.html` → `views.landing_page()`)
- User arrives at the experiment
- Randomly assigns experimental condition (ps, d'H, d'S)
- Loads trial data from CSV
- Creates database entry for participant

### **2. Consent Form** (`consent_form.html` → `views.consent_form()`)
- User reads and accepts consent
- Redirects to reCAPTCHA

### **3. Instructions** (`instructions.html` → `views.instructions()`)
- Multi-screen instructions
- Explains task, scoring, blocks
- User clicks through screens
- Starts Block 1 (practice, 10 trials)

### **4. Game/Trials** (`game.html` → `views.game()`)
- **Block 1**: 10 practice trials (no DS system)
- **Block 2**: 10 practice trials (with DS system)
- **Block 3**: 100 main trials (with DS system) - OR split into 2 blocks of 50
- For each trial:
  - Shows evidence value (number)
  - Shows DS alarm (if purchased)
  - User clicks "Intervention" or "No intervention"
  - Feedback (correct/wrong)
  - Score updates
  - Data saved to database

### **5. Questionnaires** (`toast_1.html`, `toast_2.html`)
- Post-experiment questions about system trust, usefulness, etc.

### **6. End Page** (`end.html`)
- Thank you message
- Completion status

---

## 🎯 Key Concepts

### **Models (Database Tables)**
- **ExperimentData**: Stores participant info (user_id, ps, d'H, d'S, completion status)
- **ExperimentAction**: Stores each trial response (block, trial, decision, time, correctness)
- **TOASTResponse**: Stores questionnaire responses

### **Views (Backend Logic)**
- **`load_block_trials()`**: Loads trial data from CSV based on condition
- **`game()`**: Handles each trial (displays data, saves responses)
- **`save_db()`**: Exports database to CSV files

### **Templates (Frontend)**
- HTML pages with Django template syntax (`{{ variable }}`, `{% if %}`)
- JavaScript for interactions (button clicks, feedback)

---

## 🚗 Adapting for Autonomous Cars

### **What Needs to Change:**

#### **1. Terminology & Text** (Templates)

**Current (Medical):**
- "nurse in a hospital"
- "patient needs intervention"
- "Automatic Health Monitoring System"
- "Intervention" / "No intervention"
- "Alarm" (red) / "No Alarm" (green)

**New (Autonomous Cars):**
- "driver in an autonomous vehicle"
- "situation requires braking/action"
- "Autonomous Driving Safety System"
- "Brake/Take Action" / "Continue"
- "Warning" (red) / "Safe" (green)

#### **2. Images** (`static/images/`)
- Replace glucometer images with car/dashboard images
- Update alarm images to car warning displays

#### **3. Instructions** (`templates/instructions.html`)
- Change scenario from medical to driving
- Update examples and explanations
- Keep same structure (multi-screen)

#### **4. Game Interface** (`templates/game.html`)
- Change button labels: "Intervention" → "Brake/Take Action"
- Change button labels: "No intervention" → "Continue"
- Update device image (glucometer → car dashboard)
- Keep same functionality

#### **5. Scoring Logic** (`experiment/views.py`)
- Keep same scoring system (TP, TN, FP, FN)
- Update variable names if needed (but not necessary)
- Update feedback messages if desired

#### **6. Data Structure** (No changes needed!)
- CSV format stays the same
- Database models stay the same
- Only labels/descriptions change

---

## 📝 Step-by-Step Adaptation Checklist

### **Phase 1: Text & Terminology**

- [ ] **`templates/instructions.html`**
  - [ ] Screen 1: Change "nurse" → "driver", "hospital" → "autonomous vehicle"
  - [ ] Screen 2: Change "patient" → "driving situation", "intervention" → "braking/action"
  - [ ] Screen 3: Update scoring examples
  - [ ] Screen 4: Change "Automatic Health Monitoring System" → "Autonomous Driving Safety System"
  - [ ] Screen 5: Update alarm descriptions
  - [ ] Screen 6: Update purchase decision context

- [ ] **`templates/game.html`**
  - [ ] Change button text: "Intervention" → "Brake/Take Action"
  - [ ] Change button text: "No intervention" → "Continue"
  - [ ] Update image path (if using new car images)

- [ ] **`templates/consent_form.html`**
  - [ ] Update consent text to mention autonomous driving

- [ ] **`templates/landing_page.html`**
  - [ ] Update welcome text

### **Phase 2: Images & Visuals**

- [ ] **`static/images/`**
  - [ ] Create/replace with car dashboard images
  - [ ] Create warning display images (red/green)
  - [ ] Update image paths in HTML templates

### **Phase 3: Optional Enhancements**

- [ ] **Variable names** (if desired)
  - [ ] Rename "ds_judgment" → "system_warning" (optional, requires code changes)
  - [ ] Update comments in `views.py`

- [ ] **Feedback messages** (`templates/game.html`)
  - [ ] Update "Correct classification" → "Correct decision"
  - [ ] Update "Wrong classification" → "Incorrect decision"

---

## 🔍 Files to Modify (Priority Order)

### **HIGH PRIORITY** (Must change):
1. `templates/instructions.html` - All text about medical context
2. `templates/game.html` - Button labels and image
3. `static/images/` - Replace glucometer images

### **MEDIUM PRIORITY** (Should change):
4. `templates/consent_form.html` - Update consent text
5. `templates/landing_page.html` - Update welcome text
6. `templates/end.html` - Update thank you message

### **LOW PRIORITY** (Optional):
7. `experiment/views.py` - Update comments/variable names
8. `experiment/models.py` - Update field help text (if any)

---

## 🎨 Example Text Replacements

### **Instructions Screen 1:**
**Before:**
```html
<p>Assume that you are a nurse in a hospital.</p>
<p>Your task is to monitor patients and to decide whether a patient needs an intervention or not.</p>
```

**After:**
```html
<p>Assume that you are a driver in an autonomous vehicle.</p>
<p>Your task is to monitor driving situations and to decide whether the car needs to brake or take action.</p>
```

### **Game Buttons:**
**Before:**
```html
<button onclick="handleClassification(event, 'signal')">Intervention</button>
<button onclick="handleClassification(event, 'noise')">No intervention</button>
```

**After:**
```html
<button onclick="handleClassification(event, 'signal')">Brake/Take Action</button>
<button onclick="handleClassification(event, 'noise')">Continue</button>
```

### **System Description:**
**Before:**
```html
<p>You will also receive output from an <b>Automatic Health Monitoring System</b></p>
<li><p><b>"Alarm"</b> (red) = system says intervention needed</p></li>
<li><p><b>"No Alarm"</b> (green) = system says no intervention needed</p></li>
```

**After:**
```html
<p>You will also receive output from an <b>Autonomous Driving Safety System</b></p>
<li><p><b>"Warning"</b> (red) = system says braking/action needed</p></li>
<li><p><b>"Safe"</b> (green) = system says continue driving</p></li>
```

---

## 🚀 Quick Start: Minimal Changes

**To get started quickly, just change these 3 files:**

1. **`templates/instructions.html`**: Find and replace:
   - "nurse" → "driver"
   - "hospital" → "autonomous vehicle"
   - "patient" → "driving situation"
   - "intervention" → "braking/action"
   - "Automatic Health Monitoring System" → "Autonomous Driving Safety System"
   - "Alarm" → "Warning"
   - "No Alarm" → "Safe"

2. **`templates/game.html`**: Change button text:
   - "Intervention" → "Brake/Take Action"
   - "No intervention" → "Continue"

3. **`static/images/`**: Replace glucometer images with car dashboard images

**That's it!** The rest of the code will work as-is.

---

## 📊 Data Flow (Unchanged)

```
CSV File (conditions_experiment_3ps_11x11_120_A.csv)
    ↓
load_block_trials() loads data
    ↓
User sees trial (game.html)
    ↓
User makes decision
    ↓
ExperimentAction saved to database
    ↓
Next trial or end
    ↓
save_db() exports to CSV files
```

**This flow stays the same regardless of domain!**

---

## 🧪 Testing Your Changes

1. **Start server**: `python manage.py runserver`
2. **Open browser**: `http://localhost:8000`
3. **Go through experiment**: Check all text appears correctly
4. **Check database**: Verify data is saved correctly
5. **Export data**: Use admin panel or `save_db()` view

---

## 💡 Tips

- **Keep the same structure**: Don't change the experiment logic, just the text/images
- **Test thoroughly**: Go through all screens and blocks
- **Backup first**: Make a copy before making changes
- **Incremental changes**: Change one file at a time, test, then move to next
- **CSV data stays the same**: No need to regenerate data files

---

## ❓ Common Questions

**Q: Do I need to change the CSV file?**
A: No! The data structure is domain-agnostic. It's just numbers and labels.

**Q: Do I need to change the database models?**
A: No! The models store generic data (decisions, times, correctness).

**Q: Can I change the scoring system?**
A: Yes, but it's in `views.py` in the `game()` function. Current: TP=+3, TN=+3, FN=-6, FP=-3.

**Q: How do I add new images?**
A: Put them in `static/images/` and update the `<img src="">` paths in HTML templates.

**Q: Can I change the number of trials?**
A: Yes, but you'd need to regenerate the CSV file with different N_TRIALS.

